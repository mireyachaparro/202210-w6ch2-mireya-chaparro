import { Auth } from '../components/auth/auth';

function HomePage() {
    return (
        <main>
            <h2>Home page</h2>
            <Auth></Auth>
        </main>
    );
}

export default HomePage;
